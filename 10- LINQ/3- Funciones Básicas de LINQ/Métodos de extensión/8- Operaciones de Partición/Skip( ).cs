/*El operador Skip() nos permite saltar un número especificado de elementos desde el inicio de una colección y
 tomar el resto. Aquí tienes un ejemplo:*/

 var result = collection.Skip(count);

/*Este código salta los primeros count elementos de la colección y devuelve el resto de elementos como 
un nuevo subconjunto.*/