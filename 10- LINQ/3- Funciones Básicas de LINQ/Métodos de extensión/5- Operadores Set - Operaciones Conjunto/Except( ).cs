/*El operador Except() nos permite obtener los elementos que existen en una colección pero no en otra.
 Devuelve una nueva colección que contiene los elementos de la primera colección que no están presentes 
 en la segunda colección. Aquí tienes un ejemplo:*/

var result = collection1.Except(collection2);

/*Este código devuelve una nueva colección que contiene los elementos de collection1 que no están presentes en 
collection2.*/