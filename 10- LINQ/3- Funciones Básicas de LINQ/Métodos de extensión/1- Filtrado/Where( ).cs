/*El método de extensión Where() se puede utilizar en combinación con el operador de consulta from o directamente
 en una colección para filtrar los elementos. Aquí tienes un ejemplo:*/

var result = collection.Where(element => element.Property > 10);

/*Este código también devuelve una nueva colección con los elementos que cumplen con la condición especificada.*/